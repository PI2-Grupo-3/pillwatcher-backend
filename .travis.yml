dist: trusty
language: java
jdk:
  - oraclejdk8

stages:
  - build

jobs:
  include:
  - stage: build
    script: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

cache:
  directories:
  - $HOME/.m2